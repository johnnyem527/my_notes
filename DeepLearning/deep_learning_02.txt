常见的损失函数（损失函数值越小，模型越好）：
    0-1损失函数：用于分类
    平方损失函数：用于回归，预测人口，因为是连续的
    绝对损失函数：

经验风险：
    平均损失会由于训练数据有限而导致经验风险

过拟合：
    过度分类，过度在乎分类完美

结构风险最小化：
    控制模型复杂度
    常用正则化：
        L1范式，尽可能让w为0
        L2范式，尽可能让w最小，但不为0